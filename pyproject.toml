[project]
name = "studmuffin-ledger"
version = "0.1.0"
description = "Financial automation for the studmuffin"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.54.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
